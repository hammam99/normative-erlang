This repo hosts the fiels uses to implmenent the translation of normative relations in an actor-oriented approach from eFLINT semantics into Erlang


## How to run:
`
erlc eflint.erl scenario_help_with_homework.erl
erl -noshell -s scenario_help_with_homework run -s init stop

`
